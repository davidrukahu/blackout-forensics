node_modules/
dist/
*.tsbuildinfo
.env
.env.local
coverage/
.DS_Store
.gstack/

# Never commit customer telemetry, exports or bundles. See PRD §15.4.
/customer-data/
*.bundle.tar
*.parquet
!packages/spec/fixtures/**/*.parquet

# OSM extracts are fetched, never committed: an extract in the repository is
# redistribution, and PRD 13.3 forbids bundling country data into a source release.
map-data/
*.osm.pbf

# React Router typegen and build output
.react-router/
packages/app/build/
